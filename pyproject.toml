[project]
name = "contractwatch"
version = "0.2.0"
description = "Federal contract award anomaly detection dashboard"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
]

[tool.uv]
package = false
